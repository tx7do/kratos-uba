// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"kratos-bi/app/core/service/internal/data/ent/debugdevice"
	"kratos-bi/app/core/service/internal/data/ent/predicate"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// DebugDeviceQuery is the builder for querying DebugDevice entities.
type DebugDeviceQuery struct {
	config
	ctx        *QueryContext
	order      []OrderFunc
	inters     []Interceptor
	predicates []predicate.DebugDevice
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DebugDeviceQuery builder.
func (ddq *DebugDeviceQuery) Where(ps ...predicate.DebugDevice) *DebugDeviceQuery {
	ddq.predicates = append(ddq.predicates, ps...)
	return ddq
}

// Limit the number of records to be returned by this query.
func (ddq *DebugDeviceQuery) Limit(limit int) *DebugDeviceQuery {
	ddq.ctx.Limit = &limit
	return ddq
}

// Offset to start from.
func (ddq *DebugDeviceQuery) Offset(offset int) *DebugDeviceQuery {
	ddq.ctx.Offset = &offset
	return ddq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ddq *DebugDeviceQuery) Unique(unique bool) *DebugDeviceQuery {
	ddq.ctx.Unique = &unique
	return ddq
}

// Order specifies how the records should be ordered.
func (ddq *DebugDeviceQuery) Order(o ...OrderFunc) *DebugDeviceQuery {
	ddq.order = append(ddq.order, o...)
	return ddq
}

// First returns the first DebugDevice entity from the query.
// Returns a *NotFoundError when no DebugDevice was found.
func (ddq *DebugDeviceQuery) First(ctx context.Context) (*DebugDevice, error) {
	nodes, err := ddq.Limit(1).All(setContextOp(ctx, ddq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{debugdevice.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ddq *DebugDeviceQuery) FirstX(ctx context.Context) *DebugDevice {
	node, err := ddq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DebugDevice ID from the query.
// Returns a *NotFoundError when no DebugDevice ID was found.
func (ddq *DebugDeviceQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = ddq.Limit(1).IDs(setContextOp(ctx, ddq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{debugdevice.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ddq *DebugDeviceQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := ddq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DebugDevice entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DebugDevice entity is found.
// Returns a *NotFoundError when no DebugDevice entities are found.
func (ddq *DebugDeviceQuery) Only(ctx context.Context) (*DebugDevice, error) {
	nodes, err := ddq.Limit(2).All(setContextOp(ctx, ddq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{debugdevice.Label}
	default:
		return nil, &NotSingularError{debugdevice.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ddq *DebugDeviceQuery) OnlyX(ctx context.Context) *DebugDevice {
	node, err := ddq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DebugDevice ID in the query.
// Returns a *NotSingularError when more than one DebugDevice ID is found.
// Returns a *NotFoundError when no entities are found.
func (ddq *DebugDeviceQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = ddq.Limit(2).IDs(setContextOp(ctx, ddq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{debugdevice.Label}
	default:
		err = &NotSingularError{debugdevice.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ddq *DebugDeviceQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := ddq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DebugDevices.
func (ddq *DebugDeviceQuery) All(ctx context.Context) ([]*DebugDevice, error) {
	ctx = setContextOp(ctx, ddq.ctx, "All")
	if err := ddq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*DebugDevice, *DebugDeviceQuery]()
	return withInterceptors[[]*DebugDevice](ctx, ddq, qr, ddq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ddq *DebugDeviceQuery) AllX(ctx context.Context) []*DebugDevice {
	nodes, err := ddq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DebugDevice IDs.
func (ddq *DebugDeviceQuery) IDs(ctx context.Context) (ids []uint32, err error) {
	if ddq.ctx.Unique == nil && ddq.path != nil {
		ddq.Unique(true)
	}
	ctx = setContextOp(ctx, ddq.ctx, "IDs")
	if err = ddq.Select(debugdevice.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ddq *DebugDeviceQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := ddq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ddq *DebugDeviceQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ddq.ctx, "Count")
	if err := ddq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ddq, querierCount[*DebugDeviceQuery](), ddq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ddq *DebugDeviceQuery) CountX(ctx context.Context) int {
	count, err := ddq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ddq *DebugDeviceQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ddq.ctx, "Exist")
	switch _, err := ddq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ddq *DebugDeviceQuery) ExistX(ctx context.Context) bool {
	exist, err := ddq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DebugDeviceQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ddq *DebugDeviceQuery) Clone() *DebugDeviceQuery {
	if ddq == nil {
		return nil
	}
	return &DebugDeviceQuery{
		config:     ddq.config,
		ctx:        ddq.ctx.Clone(),
		order:      append([]OrderFunc{}, ddq.order...),
		inters:     append([]Interceptor{}, ddq.inters...),
		predicates: append([]predicate.DebugDevice{}, ddq.predicates...),
		// clone intermediate query.
		sql:  ddq.sql.Clone(),
		path: ddq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreateTime int64 `json:"create_time,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.DebugDevice.Query().
//		GroupBy(debugdevice.FieldCreateTime).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ddq *DebugDeviceQuery) GroupBy(field string, fields ...string) *DebugDeviceGroupBy {
	ddq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &DebugDeviceGroupBy{build: ddq}
	grbuild.flds = &ddq.ctx.Fields
	grbuild.label = debugdevice.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreateTime int64 `json:"create_time,omitempty"`
//	}
//
//	client.DebugDevice.Query().
//		Select(debugdevice.FieldCreateTime).
//		Scan(ctx, &v)
func (ddq *DebugDeviceQuery) Select(fields ...string) *DebugDeviceSelect {
	ddq.ctx.Fields = append(ddq.ctx.Fields, fields...)
	sbuild := &DebugDeviceSelect{DebugDeviceQuery: ddq}
	sbuild.label = debugdevice.Label
	sbuild.flds, sbuild.scan = &ddq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a DebugDeviceSelect configured with the given aggregations.
func (ddq *DebugDeviceQuery) Aggregate(fns ...AggregateFunc) *DebugDeviceSelect {
	return ddq.Select().Aggregate(fns...)
}

func (ddq *DebugDeviceQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ddq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ddq); err != nil {
				return err
			}
		}
	}
	for _, f := range ddq.ctx.Fields {
		if !debugdevice.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ddq.path != nil {
		prev, err := ddq.path(ctx)
		if err != nil {
			return err
		}
		ddq.sql = prev
	}
	return nil
}

func (ddq *DebugDeviceQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*DebugDevice, error) {
	var (
		nodes = []*DebugDevice{}
		_spec = ddq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*DebugDevice).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &DebugDevice{config: ddq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(ddq.modifiers) > 0 {
		_spec.Modifiers = ddq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ddq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ddq *DebugDeviceQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ddq.querySpec()
	if len(ddq.modifiers) > 0 {
		_spec.Modifiers = ddq.modifiers
	}
	_spec.Node.Columns = ddq.ctx.Fields
	if len(ddq.ctx.Fields) > 0 {
		_spec.Unique = ddq.ctx.Unique != nil && *ddq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ddq.driver, _spec)
}

func (ddq *DebugDeviceQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(debugdevice.Table, debugdevice.Columns, sqlgraph.NewFieldSpec(debugdevice.FieldID, field.TypeUint32))
	_spec.From = ddq.sql
	if unique := ddq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ddq.path != nil {
		_spec.Unique = true
	}
	if fields := ddq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, debugdevice.FieldID)
		for i := range fields {
			if fields[i] != debugdevice.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ddq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ddq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ddq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ddq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ddq *DebugDeviceQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ddq.driver.Dialect())
	t1 := builder.Table(debugdevice.Table)
	columns := ddq.ctx.Fields
	if len(columns) == 0 {
		columns = debugdevice.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ddq.sql != nil {
		selector = ddq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ddq.ctx.Unique != nil && *ddq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range ddq.modifiers {
		m(selector)
	}
	for _, p := range ddq.predicates {
		p(selector)
	}
	for _, p := range ddq.order {
		p(selector)
	}
	if offset := ddq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ddq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ddq *DebugDeviceQuery) Modify(modifiers ...func(s *sql.Selector)) *DebugDeviceSelect {
	ddq.modifiers = append(ddq.modifiers, modifiers...)
	return ddq.Select()
}

// DebugDeviceGroupBy is the group-by builder for DebugDevice entities.
type DebugDeviceGroupBy struct {
	selector
	build *DebugDeviceQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ddgb *DebugDeviceGroupBy) Aggregate(fns ...AggregateFunc) *DebugDeviceGroupBy {
	ddgb.fns = append(ddgb.fns, fns...)
	return ddgb
}

// Scan applies the selector query and scans the result into the given value.
func (ddgb *DebugDeviceGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ddgb.build.ctx, "GroupBy")
	if err := ddgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DebugDeviceQuery, *DebugDeviceGroupBy](ctx, ddgb.build, ddgb, ddgb.build.inters, v)
}

func (ddgb *DebugDeviceGroupBy) sqlScan(ctx context.Context, root *DebugDeviceQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ddgb.fns))
	for _, fn := range ddgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ddgb.flds)+len(ddgb.fns))
		for _, f := range *ddgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ddgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ddgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// DebugDeviceSelect is the builder for selecting fields of DebugDevice entities.
type DebugDeviceSelect struct {
	*DebugDeviceQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (dds *DebugDeviceSelect) Aggregate(fns ...AggregateFunc) *DebugDeviceSelect {
	dds.fns = append(dds.fns, fns...)
	return dds
}

// Scan applies the selector query and scans the result into the given value.
func (dds *DebugDeviceSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dds.ctx, "Select")
	if err := dds.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DebugDeviceQuery, *DebugDeviceSelect](ctx, dds.DebugDeviceQuery, dds, dds.inters, v)
}

func (dds *DebugDeviceSelect) sqlScan(ctx context.Context, root *DebugDeviceQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(dds.fns))
	for _, fn := range dds.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*dds.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (dds *DebugDeviceSelect) Modify(modifiers ...func(s *sql.Selector)) *DebugDeviceSelect {
	dds.modifiers = append(dds.modifiers, modifiers...)
	return dds
}
